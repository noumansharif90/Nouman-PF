#include<iostream>
using namespace std;
main()
{
    int num;
    int sum=0;
    cout<<" Enter the 5 integers: ";
    cin>>num;
    sum=sum+num;
    cin>>num;
    sum=sum+num;
    cin>>num;
    sum=sum+num;
    cin>>num; 
    sum=sum+num;
    cin>>num;
    sum=sum+num;
    cout<<"sum= "<<sum;
    
}